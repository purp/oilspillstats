<h1>New event</h1>

<% form_for([:admin, @event]) do |f| %>
  <%= f.error_messages %>

  <%= render(:partial => "edit_fields" , :locals => {:f => f})%>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', admin_events_path %>